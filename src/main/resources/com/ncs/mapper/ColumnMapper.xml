<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.ncs.mapper.ColumnMapper"> 

<!--   <select id="searchRowCount">
  
  </select>
  <select id="searchList">
  
  </select>
  <select id="totalRowCount">
  	select count(*) from column
  </select>
  <update id="countUp">
    update column set cnt=cnt+1 where seq=#{seq}
  </update> -->
  <select id="selectList" resultType="com.ncs.vo.ColumnVO">
	select seq,title,id,regdate,cnt from columnm order by seq
  </select>
  
  <insert id="insert">
	<selectKey keyProperty="seq" order="BEFORE" resultType="int">
	select IFNULL(MAX(seq),0)+1 as seq from columnm
	</selectKey>
  	insert into columnm(seq,id,title,content) values(#{seq},#{id},#{title},#{content})
  </insert>
  
  <select id="selectOne" resultType="com.ncs.vo.ColumnVO">
	select seq,title,id,content,regdate,cnt from columnm where seq=#{seq}
  </select>
  <update id="update">
  	update columnm set title=#{title}, content=#{content} where seq=#{seq}
  </update>
<!--  <delete id="delete">
  	delete column
	<if test="seq!=root">where seq=#{seq} </if>
	<if test="seq==root">where root=#{seq}</if>
  </delete> -->
  </mapper>